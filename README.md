# Vercel access audit

What Claude can and cannot do against the Vercel API from its cloud environment, with terminal screenshots of every request (self-contained `index.html`).

Live site: https://imagine-os.github.io/vercel-audit/
